import os

BASE_PATH = "/home/poweramanita/goldantelopeasia/moto_nhatrang"
WHATSAPP = "https://wa.me/84374961375"

def main():
    if not os.path.exists(BASE_PATH):
        print("Папка не найдена")
        return

    folders = [d for d in os.listdir(BASE_PATH) if os.path.isdir(os.path.join(BASE_PATH, d))]
    
    for folder in folders:
        desc_path = os.path.join(BASE_PATH, folder, "description.txt")
        
        # Красивое название модели из имени папки
        name = folder.replace('_', ' ').replace('moto', '').strip().title()
        
        # Текст ровно на ~300-350 знаков
        text = (
            f"🛵 {name} в аренду — ваш идеальный спутник для поездок по Нячангу и живописным окрестностям! "
            f"Байк находится в отличном техническом состоянии, регулярно проходит сервисное обслуживание "
            f"и полностью готов к работе. В комплекте предоставляются два чистых шлема, дождевики и "
            f"надежный держатель для телефона. Мы обеспечиваем поддержку клиентов на протяжении всего срока аренды.\n\n"
            f"📲 Для уточнения актуальной стоимости и бронирования пишите в WhatsApp: {WHATSAPP}"
        )
        
        with open(desc_path, "w", encoding="utf-8") as f:
            f.write(text)
        print(f"✅ Обновлено описание: {folder}")

    print(f"\n🚀 Готово! Обновлено {len(folders)} папок.")

if __name__ == "__main__":
    main()
